# QuickBite Frontend Configuration

# The production backend URL for the web app to query.
# Note: For static hosting of the frontend monolith by the backend on Render, 
# requests default to the same origin. 
# This variable is primarily used when compiling the standalone Android/iOS Capacitor app.
VITE_API_URL=https://quickbite-v2-backend.onrender.com
